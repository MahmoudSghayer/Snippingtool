node_modules
dist
.turbo
coverage
pnpm-lock.yaml
playwright-report
test-results
*.min.js

# milestone-1 extension code: moved byte-identical, not reformatted here.
# The TS migration (later wave) brings it onto the shared Prettier config.
apps/extension/

# generated artifacts (regenerated by scripts; formatting them makes the
# staleness checks disagree with the generator's output)
apps/api/openapi/
apps/dashboard/src/api/schema.d.ts
.tools/
