{
  "name": "@sl/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Sniper's Ledger — MV3 extension (M1 recorder; M2 assist / M3 automation TypeScript rewrite lands in a later wave).",
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint": "echo \"@sl/extension: plain JS milestone-1 code, not yet on the shared ESLint preset (TS migration is a later wave) - skipping\"",
    "typecheck": "echo \"@sl/extension: plain JS milestone-1 code, no tsconfig yet (TS migration is a later wave) - skipping\"",
    "build": "echo \"@sl/extension: no build step yet, loads unpacked (Vite/crxjs build lands with the TS migration)\"",
    "dev": "echo \"@sl/extension: load unpacked via chrome://extensions during development\""
  }
}
