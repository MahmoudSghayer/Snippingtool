{
  "name": "ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FC transfer market recorder — an unpacked MV3 extension, no build step.",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
